name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Set up Node.js 20 (required for Capacitor 7)
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: |
          npm install -g @capacitor/cli
          npm install

      - name: ⚙️ Initialize Capacitor & Add Android (if needed)
        run: |
          npx cap init swipe-ninja com.swipeninja.app --web-dir=.
          if [ ! -d "android" ]; then
            npx cap add android
          fi
          npx cap copy

      - name: 🔧 Set up JDK 17 for Android Build
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🏗️ Build Android APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: 📤 Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: swipe-ninja-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
